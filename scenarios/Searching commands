

# 🧩 **سيناريو البحث والتحليل النصي – Search & Parse Scenario**

## **📖 مقدمة السيناريو**

أنت مسؤول نظام في شركة تعمل على مشروع تحليل للملفات باسم **LogAnalyzer**.
مطلوب منك العثور على ملفات معينة، البحث داخلها، استخراج بيانات منها، وتعديل محتوى بعض السطور.

ستستخدم أدوات قوية:
**find – grep – locate – which – whereis – awk – sed**



# 🚀 **بداية السيناريو**

---

## **📌 الخطوة 1 — إنشاء مجلد المشروع**

أنشئ مجلد باسم: LogAnalyzer
mkdir LogAnalyzer


## **📌 الخطوة 2 — الدخول للمجلد


cd LogAnalyzer

## **📌 الخطوة 3 — إنشاء 3 ملفات نصية لاختبار البحث

أنشئ الملفات الآتية: 

1. system.log
   يحتوي على:
   ERROR: Disk not found
   INFO: System is running
   WARNING: High temperature detected
   ERROR: Backup failed
   

touch system.log 

echo" ERROR: Disk not found
   INFO: System is running
   WARNING: High temperature detected
   ERROR: Backup failed " > system.log 

2. **app.log**
   يحتوي على:

   
   INFO: App started
   ERROR: Cannot load config
   INFO: App closed

touch app.log

echo" INFO: App started
   ERROR: Cannot load config
   INFO: App closed" > app.log


3. **debug.txt**
   يحتوي على:


   Debug mode enabled
   Verbose logging active
   

touch debug.txt

echo "  Debug mode enabled
   Verbose logging active" > debug.txt

## **📌 الخطوة 4 — إنشاء ملف كبير لاختبار find -size**

أنشئ ملف باسم:

* **bigfile.bin**

واجعله بحجم أكبر من 100MB (مثال):

dd if=/dev/zero of=bigfile.bin bs=1M count=100


touch bigfile.bin
dd if=/dev/zero of=bigfile.bin bs=1M count=100
# 🔍 **أوامر البحث find**


## **📌 الخطوة 5 — البحث عن ملف باسم app.log داخل مجلد المشروع**

استخدم:

```
find . -name "app.log"


## **📌 الخطوة 6 — البحث عن كل الملفات التي تحتوي كلمة .log**

```
find . -name "*.log"


## **📌 الخطوة 7 — البحث عن الملفات الأكبر من 50MB**

```
find . -size +50M

# 🕵️ **استخدام grep للبحث داخل الملفات**

## **📌 الخطوة 8 — البحث عن السطور التي تحتوي ERROR في ملف system.log**

```
grep "ERROR" system.log


## **📌 الخطوة 9 — البحث بشكل متكرر في المجلد عن كلمة INFO**

```
grep -r "INFO" .

---

---

# 📂 **استخدام locate**

---

## **📌 الخطوة 10 — البحث عن ملف اسمه bashrc في النظام**

```
locate bashrc


# 🧭 **أوامر which و whereis**

---

## **📌 الخطوة 11 — معرفة مسار الأمر nano**

```
which nano

## **📌 الخطوة 12 — الحصول على المسارات الكاملة للأمر ls**

```
whereis ls

# 📝 **تحليل النصوص بـ awk*


## **📌 الخطوة 13 — طباعة كل السطور التي تحتوي ERROR من system.log باستخدام awk**

```
awk '/ERROR/ {print $0}' system.log

## **📌 الخطوة 14 — طباعة السطر كامل من app.log لكل سطر يبدأ بـ INFO**

```
awk '/^INFO/ {print $0}' app.log



# ✏️ **تحرير النصوص باستخدام sed**

## **📌 الخطوة 15 — استبدال كلمة ERROR بكلمة ISSUE في ملف system.log**

(يتم التعديل فقط عند العرض)

```
sed 's/ERROR/ISSUE/' system.log


## **📌 الخطوة 16 — استبدال كل الكلمات ERROR في الملف وحفظ التعديل**

```
sed -i 's/ERROR/ISSUE/g' system.log


# 🧪 **اختبار شامل**

---

## **📌 الخطوة 17 — اعرض كل ملفات المشروع وأذوناتهم**

ls -lh


# 🎉 **📌 الخطوة 18 — نهاية السيناريو**



